# COMPSCI 340 / SOFTENG 370 综合速查表

- 课程：2026 S2 Operating Systems（操作系统），授课 Talia Xu
- 平台：xv6（教学用 Unix 再实现）+ QEMU（Quick Emulator，快速模拟器）
- 整合来源：内核/进程表、分页表、SOFTENG370 期中总表、2026 S1 试卷复习表
- 原则：重复只留一处；每个知识点写到「能动手算 / 能排除错选项」；专有名词一律 **中文（English / 缩写）**
- 试卷风格：闭卷、选 BEST answer；无计算器时用 2 的幂口算

**怎么用**
1. 先把第 0 节缩写表过一遍（老师口播/卷面常直接扔缩写）。
2. 正文按主题读「定义 → 机制 → 易错点」。
3. 第 12 节是已对过的错题/标准答案，以老师 Piazza / 论坛澄清为准。

---

## 0. 专有名词 · 英文 · 缩写总表

老师上课、卷面、Piazza 常只说缩写。见到不认识的词先回这里。

### 0.1 总框架

| 中文 | English | 常见缩写 / 别名 |
|------|---------|-----------------|
| 操作系统 | Operating System | OS |
| 指令集架构 | Instruction Set Architecture | ISA（本课常见 RISC-V） |
| 应用编程接口（源码级：函数名、参数类型） | Application Programming Interface | API |
| 应用二进制接口（寄存器怎么传参、系统调用号） | Application Binary Interface | ABI |
| 虚拟化 | Virtualization | — |
| 并发 | Concurrency | — |
| 持久化 | Persistence | — |
| 硬件抽象层 | Hardware Abstraction | — |
| 教学用 Unix | xv6 | xv6 |
| 快速模拟器 | Quick Emulator | QEMU |
| 学校远程 Ubuntu | FlexIT | FlexIT |
| RISC-V 硬件线程 | hardware thread | hart（启动日志：`hart 1 starting`） |

### 0.2 CPU、特权、陷入

| 中文 | English | 常见缩写 / 别名 |
|------|---------|-----------------|
| 中央处理器 | Central Processing Unit | CPU |
| 程序计数器（下一条指令地址） | Program Counter | PC（RISC-V 里也叫 `pc`） |
| 用户态 | user mode / user space | U-mode；x86 约等于 ring 3 |
| 内核态 | kernel mode / supervisor / kernel space | S-mode / kernel；x86 约等于 ring 0 |
| 模式位（教材简化：CPU 当前特权开关） | mode bit | — |
| 当前特权级 | Current Privilege Level | CPL（x86 说法） |
| 保护环 | ring | ring 0 = 内核，ring 3 = 用户 |
| 系统调用 | system call | syscall；RISC-V 指令 `ecall`；ARM `svc`；x86 `syscall`/`int 0x80` |
| 陷入（当前指令引发，同步） | trap / exception | trap、fault、exception |
| 中断（设备/时钟，异步，与当前指令无关） | interrupt | IRQ、timer interrupt |
| 缺页（页不在 RAM 或权限不够） | page fault | PF、#PF |
| 特权返回指令 | supervisor return | RISC-V `sret`；x86 `sysret`/`iret` |
| C 标准库（用户态包装，不是内核本身） | C standard library | libc |
| 直接内存访问（设备不经 CPU 搬数据） | Direct Memory Access | DMA |

三种「进内核」不要混成一个词：

- **系统调用（system call / syscall）**：用户主动请求服务（`write`、`fork`）。
- **异常 / 故障（exception / fault）**：当前指令需要处理（缺页、除零、非法地址）。
- **中断（interrupt）**：外部事件（时钟滴答、磁盘完成）。

教材常把三者合称 **陷入（trap）**：CPU 存现场、升到内核态、跳到事先登记的处理程序（handler）。

### 0.3 进程与通信

| 中文 | English | 常见缩写 / 别名 |
|------|---------|-----------------|
| 进程 | process | — |
| 程序（磁盘上的指令+静态数据，还没跑） | program | — |
| 进程控制块 | Process Control Block | PCB；xv6 里常叫 `struct proc` |
| 进程号 | Process ID | PID；类型 `pid_t` |
| 父进程号 | parent PID | PPID；`getppid()` |
| 僵尸进程（已 exit，等父 wait 收尸，PCB 还在） | zombie | Z |
| 孤儿进程（父先死，被 init 收养） | orphan | — |
| 进程间通信 | Inter-Process Communication | IPC |
| 管道 | pipe | — |
| 文件描述符 | file descriptor | fd；0=stdin，1=stdout，2=stderr |
| 标准输入 / 输出 / 错误 | standard in/out/error | stdin / stdout / stderr |
| 写时复制 | Copy-On-Write | COW |
| 上下文切换 | context switch | CS |
| 调度器 | scheduler | — |
| 就绪 / 运行 / 阻塞(睡眠) / 终止 | Ready / Running / Blocked(Sleeping) / Terminated | — |
| 时间片 | time slice / quantum | q、quantum |
| 配额（MLFQ 一层累计可用 CPU） | allotment | — |
| 周转时间 | Turnaround Time | TAT = 完成 − 到达 |
| 等待时间 | waiting time | TAT − burst |
| 响应时间 | response time | 第一次上 CPU − 到达 |
| 突发时间（这次要跑多久） | CPU burst | burst |
| 到达时间 | arrival time | arr |
| 完成时间 | completion / finish time | — |

调度算法缩写（老师极爱只写字母）：

| 中文 | English | 缩写 / 别名 |
|------|---------|-------------|
| 先来先服务（非抢占） | First Come First Served | FCFS；和 FIFO 调度是同一思路 |
| 先进先出 | First In First Out | FIFO（调度或页面置换都可能出现） |
| 最短作业优先（非抢占，要知道 burst） | Shortest Job First | SJF |
| 最短剩余时间优先（抢占版 SJF） | Shortest Remaining Time First | SRTF / SRT / STR；也叫 Shortest Job-to-Completion First |
| 时间片轮转（抢占） | Round Robin | RR |
| 多级反馈队列 | Multi-Level Feedback Queue | MLFQ |
| 完全公平调度（选虚拟运行时间最小） | Completely Fair Scheduler | CFS；看 **vruntime** |
| 最早可服务虚拟截止时间优先 | Earliest Eligible Virtual Deadline First | EEVDF（Linux 6.6+ 替代 CFS） |
| 虚拟运行时间 | virtual runtime | vruntime |
| 饥饿 | starvation | — |
| 作弊（快用完配额就 yield 想赖在高层） | gaming | — |
| 周期性提升（全部打回最高层） | boost | periodic boost |
| 缓存亲和性（留在同一 CPU，缓存更热） | cache affinity | — |
| 负载均衡（任务在 CPU 间挪动） | load balancing | — |

### 0.4 虚拟内存与页表

| 中文 | English | 常见缩写 / 别名 |
|------|---------|-----------------|
| 虚拟地址（用户指针里的数） | Virtual Address | VA |
| 物理地址（真正发给 RAM 的数） | Physical Address | PA |
| 虚拟页号（VA 高位） | Virtual Page Number | VPN |
| 物理页号 / 页帧号（PA 高位） | Physical Page Number / Page Frame Number | PPN / PFN |
| 页内偏移（VA/PA 低位，翻译时原样拷贝） | page offset | offset |
| 页（虚拟地址空间里固定大小的一块） | page | — |
| 帧 / 页框（物理内存里同样大小的一块） | frame / page frame | — |
| 页表 | Page Table | PT |
| 页表项（目录里的一行） | Page-Table Entry | PTE（课上常见 8 字节） |
| 页表页（整页都是 PTE，是目录不是程序数据） | page-table page | — |
| 数据页（代码/数据/堆/栈） | data page | — |
| 内存管理单元（硬件，VA→PA） | Memory Management Unit | MMU |
| 快表 / 翻译后备缓冲（缓存 VPN→PPN，不是页内容） | Translation Lookaside Buffer | TLB |
| 地址空间标识（给 TLB 项打进程标签，切换时可不清光） | Address-Space Identifier | ASID |
| RISC-V：根页表物理地址+模式所在寄存器 | Supervisor Address Translation and Protection | satp |
| RISC-V 39 位虚拟地址分页 | Sv39 | 4 KiB 页 + 3 级，各 9 位 VPN |
| 页表遍历（TLB miss 后从根往下读 PTE） | page-table walk | walk |
| TLB 命中 / 缺失 | TLB hit / TLB miss | hit / miss |
| 有效访存时间 | Effective / Expected Access Time | EAT |
| 按需分页（第一次访问才配物理页） | demand paging | — |
| 超额承诺（malloc 成功 ≠ 物理页已备好） | overcommit | — |
| 工作集（最近真正在用的那一组页） | working set | WS |
| 抖动（忙着换页，几乎没做正事） | thrashing | — |
| 内存耗尽 | Out Of Memory | OOM（OOM killer 会杀进程） |
| 多核上通知其他 CPU 作废旧 TLB | TLB shootdown | — |
| 作废本机/指定地址的 TLB 条目 | TLB invalidation | — |
| 二进制千字节（2^10 / 2^20 / 2^30） | kibibyte / mebibyte / gibibyte | KiB / MiB / GiB（课上常和 KB/MB 混说，按 1024 算） |

PTE 权限位（RISC-V 课上会点名，老师可能只说字母）：

| 位 | English | 含义 |
|----|---------|------|
| V | Valid | 这一项有没有映射 |
| R | Read | 可读 |
| W | Write | 可写 |
| X | Execute | 可执行 |
| U | User | 用户态能不能访问 |
| G | Global | 全局映射（切换 ASID 也不丢） |
| A | Accessed | 访问过（Clock 算法的 reference bit 一类） |
| D | Dirty | 脏页（写过，换出时要写回磁盘） |

### 0.5 置换与分配

| 中文 | English | 常见缩写 / 别名 |
|------|---------|-----------------|
| 最优置换（换未来最久才用的；需要预知未来） | Optimal / MIN / Belady | OPT、MIN |
| 先进先出置换 | FIFO replacement | FIFO |
| 最久未使用 | Least Recently Used | LRU |
| 时钟 / 第二次机会 | Clock / Second Chance | Clock |
| 引用位 | reference bit / accessed bit | R bit、A bit |
| Belady 异常（帧变多，缺页反而更多） | Belady's anomaly | FIFO 可能有，OPT/LRU 没有 |
| 首次适应 | First Fit | FF |
| 最佳适应 | Best Fit | BF |
| 最差适应 | Worst Fit | WF |
| 伙伴分配器（块大小 2 的幂，对半切，伙伴才能合并） | Buddy allocator | buddy |
| 切分 | split | — |
| 合并（相邻空闲块并回去） | coalesce | — |
| 内部碎片（分给你的比你要的大） | internal fragmentation | — |
| 外部碎片（空闲总量够，但没有连续块） | external fragmentation | — |
| 内核对象缓存分配器 | slab allocator | slab |
| 程序间断 / 堆顶 | program break | `brk` / `sbrk` |
| 内存映射 | memory map | `mmap` |

### 0.6 C / Unix 系统调用（卷面按函数名出）

| 中文含义 | 名字 | 要点 |
|----------|------|------|
| 克隆进程 | `fork()` | 父返孩子 PID（>0），子返 0，失败 −1 |
| 替换进程映像 | `exec` 家族：`execl` / `execv` / `execlp` / `execve` | 成功不返回；PID 不变 |
| 等孩子结束并收尸 | `wait()` / `wait(NULL)` | 一次只收一个；消除 zombie |
| 结束自己 | `exit()` | 之后代码不跑；变成 zombie 直到被 wait |
| 建管道 | `pipe(int p[2])` | `p[0]` 读，`p[1]` 写 |
| 读 / 写 | `read` / `write` | 空管 read 阻塞；write 是 syscall，不走 stdio 缓冲 |
| 带缓冲打印 | `printf` | 用户态缓冲，换行或退出或满了才刷 |
| 当前 / 父 PID | `getpid` / `getppid` | `pid_t parent = getpid()` 只是普通变量拷贝 |
| 休眠（阻塞，不占 CPU） | `sleep` / `nanosleep` | 对比忙等 `while (uptime()-start < ticks)` |
| 堆分配 / 释放 | `malloc` / `free` | 成功只保证 VA 可用 |
| 开/关文件 | `open` / `close` | xv6 `touch` 作业会用 |
| xv6 开机 tick | `uptime` | 约 0.1 s / tick |

`execl` 的 `l` = list：参数一个个列出，必须以 `NULL` 结尾。`a.out` 只是 Unix 编译器默认输出文件名（assembler output），不是语法。

### 0.7 内核结构

| 中文 | English | 要点 |
|------|---------|------|
| 宏内核 | monolithic kernel | 调度、VM、驱动、FS 都在内核；交互快，故障影响面大 |
| 微内核 | microkernel | 特权核心很小；驱动/FS 在用户态用消息通信；隔离好，陷入/IPC 开销大 |
| 内核代码 | kernel code | 调度器、`sys_write`、trap handler、驱动…… |
| 内核内存 | kernel memory | 内核代码+数据+内核栈+进程表+页表等 |

---

## 1. 三条主线（Lecture 1–2）

操作系统干三件大事：

1. **虚拟化（Virtualization）**  
   给每个程序一份更简单的「好像独占硬件」视图。  
   - CPU → **进程（process）**  
   - 内存 → **私有虚拟地址空间（private virtual address space）**  
   - 设备 → 有名字的资源（文件、管道……）

2. **并发（Concurrency）**  
   多个活动在同一份硬件上都能往前推进。靠调度（scheduling）和上下文切换（context switch）。

3. **持久化（Persistence）**  
   程序或机器停了，数据还在。靠文件、磁盘。

一次用户请求可能穿过：

```
应用程序 → 库 libc（malloc / printf / read 的包装）
        → 内核（syscall：校验、办事）
        → 硬件（MMU / 设备 / 时钟）
```

**不一定进内核**

- `strlen()`：纯用户态算长度。
- 小 `malloc()`：堆里已有空闲块时不 `sbrk`。
- 普通 load/store：只走 MMU/TLB；命中就不 trap。

**一定进内核**

- `read` / `write` / `open` / `fork` / `exec` / `pipe` / 堆要扩张时的 `sbrk`
- 缺页、保护错、系统调用、时钟中断

Lecture 13 三个复盘问题（任何机制都能套）：

1. 跨了哪条边界？（函数调用 / trap / IPC / VA→PA）
2. 哪块状态变了？（Running→Blocked，旧程序→新程序，页变驻留）
3. 谁被用了或转走了？（CPU、物理帧、fd、管道字节、子进程退出码）

---

## 2. C 指针、栈、堆、对齐、缓冲

### 2.1 `*` 和 `&` 是两套完全不同的用法

- `&`：**取地址（address-of）**。`&z` = 变量 `z` 在（虚拟）内存里的地址。永远是这个意思。
- `*` 声明：`int *p;` 表示 `p` 是「指向 `int` 的指针（pointer）」。
- `*` 表达式：`*p` 是 **解引用（dereference）**，取该地址上的值。

`z` 还活着时：`p == &z`，`*p == z`。

用户程序里的 `&z`、`malloc` 返回值、数组下标，**全部是虚拟地址 VA**，不是物理地址 PA。

### 2.2 悬空指针（dangling pointer）与未定义行为（Undefined Behaviour / UB）

```c
int *make(void) {
    int z = 11;      /* z 在栈（stack）上，函数返回后栈帧失效 */
    int *p = &z;
    return p;        /* 把已死变量的地址交出去 */
}
```

- 栈由函数调用自动管理，生命周期跟函数走。
- **返回这个地址本身还不立刻是 UB**；之后 `printf` / `*q` **解引用** 这个悬空指针才是 UB（可能打印 11、垃圾、或崩溃）。
- 2026 Q1：老师口径是 UB 发生在解引用时，不是 `return` 那一刻。

正确做法：向 **堆（heap）** 申请，活到 `free`：

```c
int *p = malloc(sizeof(int));   /* sizeof(int) 常见 4 字节 */
if (p == NULL) return NULL;     /* malloc 失败返回 NULL */
*p = 11;
return p;                       /* OK：这是堆地址，调用方用完 free(p) */
```

口诀：`&z` 拿已有变量的地址（变量必须还活着）；`malloc` 新造一块堆内存（活到 `free`）；`*p` 顺着地址读写。

### 2.3 两个进程的全局变量也不共享

每个进程有自己的：代码、全局数据段、堆、栈、页表。  
fork 之后布局可以看起来一样（同一个 VA），但那是 **两套地址空间**。改自己的全局 `g` 不会改到另一个进程的 `g`（除非走共享内存，或还停留在 COW 未写阶段的只读共享——一旦写入就分开）。

### 2.4 结构体对齐（alignment）

常见 LP64：`int` = 4，指针 = 8。规则：

1. 每个成员对齐到 **自己的对齐要求**（`int` 要放到 4 的倍数地址）。
2. 整个结构体大小必须是 **最大成员对齐** 的倍数（做成数组时下一元素才能对齐）。
3. 最后一个成员后面也要补 padding。

例（2025 Q10）：`char a; int b; char c; int d;`  
`a(1)+pad3 + b(4) + c(1)+pad3 + d(4) = 16`

例（2026 Q25）：`char, char, int, char, int, char`  
不是 17。最后那个 `char` 后面还要按 `int` 的 4 字节对齐补到 **20**。

### 2.5 `printf` 有缓冲，`write` 没有

- `printf`：走 **stdio 用户态缓冲**。stdout 接到终端时常是行缓冲（遇 `\n` 才刷）；接到管道/文件常是全缓冲。进程退出也会刷。
- `write(fd, buf, n)`：直接系统调用，立刻交给内核。

因此：

```c
printf("Hello");
write(1, "World", 5);
printf("Again");
```

输出是 **WorldHelloAgain**（2025 Q8）。

```c
printf("14");
sleep(1);
write(1, "23", 2);
```

先看到 **23** 再看到 **14**，即 **2314**（2026 Q7）。  
`execl` 成功不返回：后面的 `printf` 永远不会执行。

---

## 3. 用户态 / 内核态 / 内核代码 / 内核内存

这四个词 **不是同一层**：

| 词 | 它是什么 |
|----|----------|
| **模式 mode** | CPU **此刻**的权限开关（硬件状态） |
| **内核代码 kernel code** | 内核的程序文本（软件） |
| **内核内存 kernel memory** | 内核占用的内存区域（代码+数据+内核栈+页表+进程表……） |
| **用户空间 user space** | 你的代码、堆、栈；页表标成用户可访问的那些 VA |

### 3.1 用户态（user mode）能做什么、不能做什么

能：算术、跳转、访问「当前进程页表允许的」虚拟地址。例如 `total += values[i];` 不必进内核。

不能：

- 执行特权指令（改页表、关中断、直接碰设备、改 `satp`）
- 访问 kernel memory（即使用户猜到内核虚拟地址，MMU 也会按权限拒绝 → page fault → 通常 Segmentation fault 杀掉进程）
- **自己把 CPU 拨到 kernel mode**（否则谁都能提权）

用户态不是「没有物理内存」，而是 **不被允许直接命名物理内存**。

### 3.2 内核态（kernel mode）

跑受信任的 OS 代码。可以控制设备、改页表、调度、处理中断。一次写错可能崩整机。

### 3.3 为什么要分两态（2025 Q22 口径）

对的理由：

- 阻止用户程序执行特权指令
- 把用户程序和关键系统操作隔开

错的理由：

- 「为了让所有程序直接碰硬件」——正好相反
- 「OS 不能访问用户内存」——OS **必须**能 `copy_from_user` / `copy_to_user`（copyin/copyout）才能完成 `read`/`write`

### 3.4 系统调用不是普通函数调用（必考）

正确模型分三层：

- **API**：名字和参数类型，如 `write(fd, buf, n)`
- **ABI**：系统调用号放哪个寄存器、参数放哪、返回值在哪
- **ISA**：受控入口指令：`syscall` / `svc` / `ecall`

真实路径：

```
用户执行 trap 指令（此时还在 user mode）
  → CPU 硬件：存现场、改 mode bit 升到 kernel mode、跳到预定 handler
  → 内核：校验请求、办事、准备返回值
  → 特权返回指令（sret / sysret / iret）
  → CPU 把 mode 改回 user mode
```

参数在哪：

- 小参数（fd、长度、syscall 号）→ **寄存器**
- 缓冲区 → **用户虚拟地址**（指向 user memory 的指针）
- 内核必须校验 + `copy_from_user` / `copy_to_user`
- 用户绝不能「准备一块 kernel memory 再把指针递进去」

**错句对照（老师确认 2026 Q5：(3) 为 false，答案 C 不是 D）**

1. 「系统调用一定从用户态转到内核态」→ **对**
2. 「syscall 就是普通函数调用进内核，参数必须是指向 kernel memory 的指针」→ **错**  
   只能进 approved entry point；参数是寄存器 + 用户指针。
3. 「内核在执行 syscall 时改 mode bit」→ **错**  
   从 user→kernel 改 mode bit 的是 **CPU 硬件**。执行 `ecall` 那一瞬间还在 user mode，内核代码还没跑。用户态没有权限自己拨高 mode bit。内核可以在已经处于 kernel mode 时，用返回指令让 CPU **降权**。

讲义原话大意：CPU pauses the current code, enters kernel mode, and starts the handler. A system call transfers control to an approved kernel entry point.

### 3.5 上下文切换（context switch）vs 模式切换（mode switch）

| | 模式切换 | 上下文切换 |
|--|----------|------------|
| 换什么 | 同一进程的 user ↔ kernel | 换 **哪个进程** 占用 CPU |
| 是否必须内核态 | 进入内核时由硬件完成 | **必须在内核态** 才能换进程 |
| 何时发生 | syscall / exception / interrupt | 中断处理完、syscall 返回前、主动 yield、阻塞 |

2025 Q15：

- (1) 上下文切换必须在内核态 → **真**
- (2) 只能发生在处理完中断后 → **假**（syscall、yield、阻塞也能切）
- (3) 用户进程 syscall 一定 user→kernel → **真**  
答案 D（真假真）。

时间片到 → 时钟中断 → 内核强制切换（involuntary preemption）。没有 **定时器硬件（timer）** 做不到真正的抢占。

### 3.6 宏内核 vs 微内核（2026 Q6）

- **性能好、服务紧耦合** → 宏内核（monolithic）：少一次用户/内核往返
- **隔离好、驱动崩溃不拖垮整机** → 微内核（microkernel）：服务在用户态，靠 IPC
- 答案口径：性能 = Monolithic；隔离 = Microkernel；紧耦合 = Monolithic

---

## 4. 进程、fork / exec / wait、管道

### 4.1 程序 vs 进程

- **程序（program）**：文件里的指令 + 静态数据，还没跑。
- **进程（process）**：正在跑的程序 + 寄存器 + 地址空间 + OS 管理的资源/权限。

要暂停/恢复一个进程，OS 必须保存：寄存器和 PC、栈/堆/代码/数据、打开的资源和其他元数据（metadata）。这些大多记在 **PCB** 里。

### 4.2 进程状态机（2026 Q11）

典型路径：

```
Created → Ready → Running → Blocked → Ready → Running → Terminated
```

- **Ready（就绪）**：想跑，在等 CPU。
- **Running（运行）**：正在占用 CPU。
- **Blocked / Sleeping（阻塞）**：等事件（空管 `read`、`sleep`、磁盘）。**不占 CPU**。
- **Terminated / Zombie**：已 `exit`，PCB 还在，等父 `wait`。

忙等（2025 Q3）：

```c
while (uptime() - start < ticks)
    ;   /* 空转 */
```

`uptime` 是 syscall，时钟中断仍会加 tick，所以 **时间能等到**；但进程一直 Running，**浪费 CPU**。真正的 `sleep()` 把进程设为 Blocked。答案是「能睡够但占 CPU」，不是「睡不够」。

### 4.3 `fork()` 细则

- 克隆当前进程：子进程得到一份地址空间视图（现代 OS 用 **COW**：先共享物理页，写时再拷）。
- 调用瞬间还是 1 个进程；**返回后**已经是 2 个。
- `if (fork() == 0)` 的意思是「**先 fork，再各自用返回值判断**」，不是先判断再分裂。
- 父子谁先被调度：**不确定**。不要假设父一定先跑。有 `pipe` / `wait` 才可能锁死顺序。
- `n` 次不被 `exit`/`exec` 剪枝的 `fork` → 最多 `2^n` 个进程。

**fork 之后 COW 三句判断（2025 Q16，答案 D：假、假、真）**

1. 各自一份页表？课上口径：**FALSE**（一般不共享页表本身；各有页表，但可以指向同一批物理页）
2. 共享虚拟内存？**FALSE**（两套地址空间，布局可以相同）
3. 共享物理页？**TRUE**（直到某一方写入，才真正拷贝）

COW 写入细节（图书馆那份考题，老师选 C）：

- 两边先把共享页标 **只读**。
- 谁先写 → page fault → 给写入方复制一页并改成可写。
- 另一方再写时：若原页已只被自己映射，**不必再复制**，直接改成可写。
- 只有「原页仍被多个映射共享」时才需要第二次复制。
- 旧的「可写」TLB 可能还留在某些 CPU 上，回用户态前必须 **TLB invalidation**；多核还要 **TLB shootdown**。

### 4.4 `exec` 家族

用新程序 **替换当前进程镜像**。PID 不变（换灵魂不换身份证），代码/数据/堆栈全换。

- 成功：**不返回**，后面的语句是死代码。
- 失败：返回 −1，后面代码会跑（所以失败分支要 `perror` + `exit`）。

```c
execl("./a.out", "./a.out", NULL);
/* l = list；第一参是路径；后面是新程序 argv[]；必须以 NULL 结尾 */
```

典型 shell：

```c
if (fork() == 0) {
    execl("/bin/ls", "ls", NULL);
    perror("execl");
    exit(1);
}
```

`ls`（list）：普通用户程序，通常 `/bin/ls`，跑完就 exit，不会无限繁殖。  
对比：`execl("./a.out")` 若 `a.out` 自己又 `fork`+`execl("./a.out")`，会 **无限指数增长**。

### 4.5 `getpid` 陷阱

```c
pid_t parent = getpid();  /* 只是把「此刻自己的号」存进普通变量 */
fork();
if (getpid() == parent)   /* 只有最初那个进程为真 */
```

fork 之后这个数字会被 **拷贝** 给孩子。孩子变量里写的仍是「最初那个进程的号」，**不是** 孩子自己的 PID，也 **不是** `getppid()`。

### 4.6 `wait` / `exit` / zombie

- 子进程 `exit` 后 **立刻变 zombie**，不是立刻从进程表消失，也还不是 orphan。
- 父进程 `wait` 收尸后，PCB 才释放。
- `wait` 一次 ≠ 等所有孩子。
- 父先死：孩子变 orphan，被 `init`（PID 1）收养，`init` 负责 wait。

2025 Q5 = E（exit 后是 zombie）。

### 4.7 管道 `pipe`

```c
int p[2];
pipe(p);           /* p[0] 读端，p[1] 写端；刚创建时管道为空 */
read(p[0], &x, 1); /* 有数据：读走；空管+写端还开着：阻塞睡着 */
write(p[1], "x", 1); /* 投 1 字节，可唤醒正在 read 的进程 */
```

- 用户调用 `pipe` → trap 进内核 → 识别 syscall 号 → `sys_pipe` → 内核分配 pipe 缓冲，返回两个 fd。
- 不是用户态自己映射共享页，也不是等两端都 `open` 才返回（2025 Q1 = A）。
- **空管 `read` 必睡**；用数据依赖锁输出顺序，不要赌调度。
- 写端全关、管空：`read` 返回 0（EOF）。

fd 约定：0 stdin，1 stdout，2 stderr；fd 也可指向文件、pipe、设备、socket。fork 后子进程 **继承** 打开的 fd。

### 4.8 分析 fork 题的固定套路

不要猜调度顺序。列出：

1. 会生出几个进程（每个成功 `fork` 让「执行到这句的进程」各变两个）
2. 每个进程会执行哪些语句（`exit` / `exec` 会切掉后面）
3. 哪些输出有数据依赖（空管 `read`、`wait`）
4. 变量是每进程一份，不是共享内存
5. 遇到 `||` / `&&`：**短路**。左边已决定真假，右边的 `fork` 可能根本不执行
   - `if (fork())`：父进分支（非 0），子不进
   - `if (!fork())`：子进分支

2025 Q29：

```c
if (fork() || fork())
    fork();
print("1");
```

- 父：左边已真，不再做第二个 `fork`，但会做第三个
- 第一个子：左边为假，必须做第二个 `fork`，再做第三个
- 第二个 fork 产生的那个进程：条件为假，不再做第三个  
共 **5** 个进程，输出 `11111`。

---

## 5. 调度（Scheduling）

### 5.1 非抢占 vs 抢占

- **非抢占（non-preemptive）**：只有进程自己结束 / 阻塞 / yield 才让出 CPU。FCFS、SJF。
- **抢占（preemptive）**：OS 可强制切走。RR 时间片到、SRTF 来了更短的、时钟中断。真正抢占需要 **定时器（timer）**。
- 时间片越小 → 切换越频繁 → **开销越大**（不是越小）。

平局：题目写 `Use FCFS for all tiebreaks` → 到达更早的优先。「P4 arrives after P3」即使同一时刻也是 P3 先入队。

### 5.2 指标怎么算

- 完成时间 finish
- **周转时间 TAT** = finish − arrival
- **等待时间** = TAT − burst
- **响应时间** = 第一次上 CPU 的时刻 − arrival
- 平均 TAT 最短：在「知道剩余时间」的前提下通常是 **SRTF**

饥饿（starvation）：

- **FCFS、RR 能保证最终被调度**（只要队列有限、时间片 > 0）
- **SJF、SRTF 可能饿死** 长作业：短作业一直来，长的永远排不上  
2026 Q18 = C

### 5.3 甘特图动手顺序

1. 先标每个进程的到达
2. 非抢占：每次在 **已经到达的就绪队列** 里按规则挑一个，跑完再挑
3. 抢占：每个 **到达点** 和 **时间片边界** 重新比
4. 平局 FCFS：先到或先入队的赢
5. 不要凭印象；一定自己画时间线

### 5.4 两套考题数据（不要混）

**2025 试卷** P1 arr=0 burst=4；P2 a=1 b=6；P3 a=2 b=2；P4 a=2（在 P3 之后）b=1

| 算法 | 完成时间 P1,P2,P3,P4 | 平均 TAT |
|------|----------------------|----------|
| FCFS | 4, 10, 12, 13 | 8.5 |
| SJF  | 4, 13, 7, 5 | 6 |
| SRTF | 5, 13, 7, 3 | **5.75 最短** |
| RR q=3 | 10, 13, 8, 9 | 8.75 |

SRTF 关键：t=2 时 P1 剩余 2，P4 剩余 1，**P4 抢占 P1**。

**2026 S1 试卷** P1(0,3) P2(1,6) P3(2,3) P4(2 after P3, 1)

| 算法 | 完成时间 |
|------|----------|
| FCFS | 3, 9, 12, 13 |
| SJF  | 3, 13, 7, 4 |
| SRTF | 3, 13, 7, 4（这组数据和 SJF 完成时间碰巧相同，但中途抢占行为不同） |
| RR q=2 | P1=8, P2=13, P3=11, P4=7 |

2026 Q16 你曾选错：SRTF 完成是 **3,13,7,4** 不是另一组。

### 5.5 多级反馈队列 MLFQ

规则：

1. 先跑当前 **最高非空** 队列
2. 同队列 RR
3. 在某一层把 **配额 allotment** 用完 → 降到下一层
4. 每隔周期 S，所有就绪进程 **boost** 回最高层，配额重置

两个时间不要混：

- **quantum（时间片）**：这一层 RR 切片，到点换人，**不一定降级**
- **allotment（配额）**：这一层累计可用 CPU，用完才降级

累计：中途 block / yield **不会**把配额清零（防 gaming）。  
Boost 后 CPU-bound 进程会 **再次一层层降下去**。Piazza：课件示意图 boost 后黑块连跑 3 倍配额还不降，是画错了。Talia：应 reset 后再 gradually downgrade。

MLFQ 要解决：

- **饥饿 starvation**：高层不断来新活 → periodic boost
- **作弊 gaming**：快用完配额就 yield 想赖在高层 → 配额跨 turn 累计

### 5.6 多核队列与 Linux 调度演变

| 设计 | 优点 | 缺点 | 适合 |
|------|------|------|------|
| 全局一条队列 | 简单、天然负载均 | 扫描慢、抢同一把锁、破坏缓存 | 短任务、课上早期 Linux Global O(N) |
| 每 CPU 一条且少迁移 | 快、cache affinity 好 | 可能不均 | 长寿命、反复用同一批数据的 worker（2026 Q19 Design B） |
| 每 CPU 一条但频繁迁移 | 负载更均 | 缓存变冷、TLB shootdown | 短任务、负载剧烈变化 |

Linux 课上口径：

- Global O(N)：一个大队列，每次全扫
- Per-CPU O(1)：每核自己排队，选下一个是常数时间
- **CFS**：选 **vruntime** 最小的（谁目前吃得最少谁上）；权重大的真实时间可以跑更久才赶上
- **EEVDF**：lag ≥ 0 才 eligible（该得到服务），再选虚拟截止时间最早的（更直接表达延迟）

---

## 6. 虚拟内存、页表、地址翻译

### 6.1 30 秒总览

进程看到的是 **虚拟地址 VA**，RAM 用的是 **物理地址 PA**。  
MMU 用页表把 **虚拟页号 VPN** 换成 **物理页号 PPN**，**页内偏移 offset 原样拷贝**。  
页表自己也占物理页。能给程序数据用的帧 = 总帧 − 内核 − 页表页 − 已占用数据页。

```
用户指令里的 VA
    → TLB 先查 VPN→PPN
    → 没有则 page-table walk
    → PA = PPN || offset
    → RAM
```

原因（virtualization + 隔离 isolation）：

1. 用户不能任意写别人的内存或内核
2. 私有地址空间：同一程序跑两份，`&local` 可以打印相同数字，但映射到不同物理页
3. 内核可在底下搬迁 / 共享 / 换页，用户无感（fork 的 COW、共享 libc 只读页、缺页再配物理页）

**Matching virtual addresses need not share physical storage.** 相同 VA ≠ 同一块物理内存。  
也可以反过来：不同进程相同 VA 映射 **同一** 物理页（共享库、COW、共享内存）。

虚拟地址空间可以 **大于** 物理内存（按需分页 demand paging），也可以小于。有效 VA 不必时刻在物理内存里（可换出）。

### 6.2 基础量（先写在卷子角落）

| 量 | 课上默认 | 370 期中也出现过 |
|----|----------|------------------|
| 页大小 | 4 KiB = 4096 = 0x1000 = 2^12 → offset **12 位** | 512 B = 2^9 → offset **9 位** |
| 十六进制 | 页 0x1000 时，最低 **3 个 hex 位** 是 offset | 页 0x200 时最低 hex 要按 9 位二进制切，不要按「整齐字节」切 |
| PTE | 8 字节（64 位） | 也考过 1 字节 PTE |
| 一张页表页能装 | 4096/8 = 512 = 2^9 项 → 一层索引 **9 位** | 512/8 = 64 = 2^6 项 → 一层索引 **6 位** |

**Sv39**：VA 39 位，offset 12，VPN 27。一层 9 位 → 3 层。单层表：2^27 × 8 B = **1 GiB**（所以必须多层）。

30 位 VA + 4 KiB + 8 B PTE：offset 12，VPN 18，9+9+12 → **正好两层**。

### 6.3 地址怎么拆

```
VA = [ VPN | offset ]
PA = [ PPN | offset ]     // offset 原样拷贝
```

例：4 KiB 页，VA `0x3A7C` = VPN `0x3` | offset `0xA7C`；页表 `0x3 → 0x9`；PA = `0x9A7C`。

多层时 VPN 再切成各层索引，offset 仍然不动：

- 30-bit VA / 4 KiB / 8B PTE：**[ 9-bit L1 | 9-bit L0 | 12-bit offset ]**
- 30-bit VA / 512 B / 8B PTE（2026 期中）：一张表 64 项 → 中间层 6 位；VPN 21 位不能被 6 整除，根剩 3 位：  
  **[ 3 | 6 | 6 | 6 | 9-bit offset ]** 共 4 层（课上习惯从高到低叫 L3 L2 L1 L0）

2025 Q14：24-bit 地址 `0x789ABC`，页 128 B = 2^7  
页号 = `0x789ABC >> 7` = `0xF135`。选项里没有 → None。陷阱：不要取高字节 `0x78`，也不要当 offset 是整数字节。

### 6.4 一次翻译在干什么

1. 程序发出 VA
2. TLB 先看有没有 VPN→PPN  
   - 有 → **TLB hit** → 直接拼 offset，访问数据页  
   - 没有 → **TLB miss** → page-table walk
3. walk：用 VPN 各段当索引，从根表（`satp` 里的根物理地址）往下读 PTE
4. 最后一层 PTE 给出 PPN + 权限位
5. PA = PPN || offset，去数据页读/写那个字节
6. 把 VPN→PPN 填进 TLB

PTE 有效且页在 RAM → 翻译成功。  
PTE 说不在 RAM / 没映射 / 权限不对 → **page fault**，进内核。

### 6.5 数据页 vs 页表页（必考分界）

| | 数据页 data page | 页表页 page-table page |
|--|------------------|------------------------|
| 装什么 | 代码、全局数据、堆、栈、文件映射 | 整页都是 PTE，是目录 |
| 谁指向它 | 最底层 PTE 的 PPN | 上一层 PTE 的 PPN（根在 satp） |
| 谁读写 | 用户最终读写的是它 | MMU 翻译时读的是它 |

两者都是同样大小的物理帧，只是内容不同。  
**数据页的 4096（或 512）字节不会被 PTE 挖走一块。PTE 单独住在页表页里。**

总占用帧 = 数据页数 + 页表页数（再加上题目单独列出的内核）。

某一级的一个 PTE 指向的是「**下一级整张页表**」，不是下一级的某一个格子。

### 6.6 为什么虚拟页和物理页 offset 一样

页和帧被规定成一样大。一页里的第 k 个字节，搬到哪一帧，仍是那一帧的第 k 个字节。硬件因此可以「只换页号，低位原样拷贝」。  
变的是住在哪一栋楼（页号），不变的是房间号（offset）。

### 6.7 PTE 不算在 VPN 里，也不算在 offset 里

PTE **不是 VA 里的一段字段**。

- VPN = 钥匙，用来当页表下标，把 PTE 找出来
- PTE = 抽屉里的纸条，写着 PPN 和权限
- offset = 找到那一页之后，取第几个字节

两个「页内偏移」不要混：

1. **VA 自带的 offset**：只用在最后的 **数据页**
2. **读页表时的页内偏移**：这一层索引 × PTE 大小（例如索引 5 × 8 = 40），用来在页表页里定位那一条 PTE。这 **不是** VA 末尾那个 offset。

### 6.8 「要除掉」——三种完全不同的除/减

错觉：物理页 4096 字节里要扣掉若干字节给 PTE。  
真相：数据页整页给数据。下面三种才是「除掉」：

1. **除法**：一张页表页能装几项 = 页大小 / PTE 大小  
   4 KiB / 8 B = 512 → 一层索引 9 位；512 B / 8 B = 64 → 6 位
2. **减法**：能给数据用的帧 = 总帧 − 内核帧 − 已分配页表页 − 已占用数据页  
   页表占的是 **整帧**，不是帧里的几个字节
3. **位域**：64 位 PTE 里一部分是 flag（V/R/W/X/U/G/A/D）。取出 PPN 时丢掉这些 flag。不影响数据页仍然是整页。

### 6.9 层数怎么来

不是「VPN 这个整数 ÷ PTE 字节数」。  
是「VPN 有多少 **位**，一层一次只能看其中几位」。

```
一层索引位数 = log2(页大小 / PTE大小)
VPN 位数     = VA位数 − offset位数
层数         ≈ ceil(VPN位数 / 一层索引位数)
```

除不尽就再加一层，或让根表项数少一点（根索引短一点）。

| 参数 | 计算 | 层数 |
|------|------|------|
| 30-bit VA，4 KiB，8B PTE | VPN=18，一层 9 位，18/9=2 | 2 |
| 39-bit VA，4 KiB，8B PTE（Sv39） | VPN=27，一层 9 位，27/9=3 | 3 |
| 30-bit VA，512 B，8B PTE | offset=9，VPN=21，中间 6 位，根剩 3 位 | 4 |
| 25-bit VA，128 B 页，PTE 1 B（2025） | offset=7，VPN=18，每级 128 项=7 位，ceil(18/7)=3 | 3 |

**页表「必须两层」不是宇宙定律。** 这题两层只是因为 18/9=2。

2026 Q21：每级页表大小 = **一帧 = 512 B**，不是 4 KiB。题目给了 512 B 页，页表必须塞进一页。

### 6.10 多层为什么省空间

单层：每个可能的 VPN 都要预留一个 PTE。Sv39：1 GiB 页表，哪怕进程只用了几页数据。

多层：上层某项 unmapped → 整棵子树的页表页都不分配。

Lecture 10 例子：30-bit VA，只用 1 个数据页

- 单层 = 2^18 × 8 B = 2 MiB
- 两层 = 1 张 L1 + 1 张 L0 = 8 KiB

省的是「没用到的那些下层表」。地址很密、几乎每页都用时，多层省得少，walk 还更慢 → 所以要 TLB。

单级 PTE 数 = 2^(VPN位数)。2025 Q18：2^18；2026 Q20：2^21。

### 6.11 最少 / 最多页表页

口诀：

- 数据页越 **挤**（VPN 高位相同）→ 共享上层表 → 页表页少
- 数据页越 **散**（高位都不同）→ 每层都可能各长一张新表 → 页表页多
- 根表通常只有 1 张
- 叶子表最多可以「一页数据配一张」（受该层项数、根的扇出限制）
- 每张已分配的表都占满 **一整页/一整帧**（哪怕只用了一项）

数「已经碰到的几个地址占了几张表」：从高位往低位看，索引相同 = 共享那一层的同一张表；索引第一次不同 = 从那一层开始分叉，多一张新表。

**2026 Q24 / Tutorial 4**（老师确认最大值 39）：  
30-bit VA，512 B 页，8B PTE，4 层，根只有 8 项，映射正好 10 个数据页。

- 最少：10 个数据页塞进同一张 L0（64 项够用）  
  1 root + 1 L2 + 1 L1 + 1 L0 + 10 数据 = **14**
- 最多：根扇出最多 8 张 L2；L1、L0 可以各 10 张  
  1 + 8 + 10 + 10 + 10 数据 = **39**

问「page table 已分配多少 **字节**」：只数页表页，**不数数据页**。每张 = 一帧。

**2026 Q23**（老师 / 论坛澄清：按 3|6|6|6|9 是 **6 张页表 = 3072 B = E**）

三个地址：`0x08418812`, `0x08418A1A`, `0x0842021F`

按 3|6|6|6|9 切 30 位：

| 地址 | L3 | L2 | L1 | L0 |
|------|----|----|----|----|
| 0x08418812 | 1 | 2 | 3 | 4 |
| 0x08418A1A | 1 | 2 | 3 | 5 |
| 0x0842021F | 1 | 2 | 4 | 1 |

- 三个都共享 L3=1、L2=2 → 1 张 L3 + 1 张 L2
- 前两个 L1=3 共享一张 L1；第三个 L1=4 另需一张 → **2 张 L1**
- 前两个同一张 L1 下不同 L0 项（4 和 5）→ **共用一张 L0**；第三个另一张 L1 → **另需一张 L0** → 2 张 L0

页表页 = 1+1+2+2 = **6**；6 × 512 = **3072 B**。

易错：以为三个地址 L1 都相同，会算成 5 张 / 2560 B。旧笔记里出现过这个错法，**以老师澄清的 6 张为准**。L0 是页表不是数据，必须计入。

**2025 Q18–20**：25-bit VA，页 128 B，PTE 1 B，130 个数据页

- 单级 PTE 数 = 2^18
- 每级 128 项 → 7 位，ceil(18/7)=3 级
- 叶表每页 128 项 → 至少 2 张 L0，再加 1 张中间 + 1 张根 = 3 张页表；总共 130+3=**133 页**

情景题（4 MiB 物理内存，4 KiB 页，30-bit VA 两层，内核 64 帧，用户 20 个数据页）：

- 总帧 = 4 MiB / 4 KiB = 1024；20 < 512，可共享一张 L0
- 最小页表：1 L1 + 1 L0 = 2；占用 64+20+2=86；还能当数据页的帧 = 1024−86=938
- 最大页表：1 L1 + 20 张 L0 = 21；占用 105；还能当数据页 = 919
- 为什么不是 1024−20：内核帧和页表帧都不能再装用户数据

### 6.12 缺页之后

合法但页不在 RAM：

1. MMU 陷入内核
2. 内核确认访问合法
3. 有空闲帧 → 拿来用；没有 → 置换算法选牺牲页（victim）
4. 从磁盘装入或填零，改 PTE，处理 TLB
5. **重试那条指令**（不是跳过）

空闲帧被页表占掉后，能拿来满足缺页的数据帧就更少。

对照：

- `fork`：新进程身份
- 成功 `exec`：当前进程换了程序
- context switch：换谁占用 CPU
- demand-page fault：一个合法映射变成驻留（resident）

---

## 7. TLB 与 EAT

### 7.1 三个词不要当成一个

| 现象 | 含义 | 页在不在 RAM |
|------|------|----------------|
| **TLB hit** | 翻译已缓存，不去读页表页，直接访问数据页 | 在（至少这次能用） |
| **TLB miss** | 翻译没缓存，要 walk 页表 | **可能已经在 RAM** |
| **page fault** | 当前 PTE 完不成这次访问（没映射 / 不在 RAM / 权限） | 不在或无权限 |

**TLB miss ≠ page fault。**  
TLB 缓存的是「最近的 VPN→PPN 翻译」，**不是页内容本身**（2026 Q27）。

多级 vs 单级：**不天生**改变 TLB miss rate。TLB 看的是最终 VPN→PPN，不看中间走几级（2025 Q6 / 2026 Q30）。

上下文切换：不同进程同一个 VPN 可以对应不同 PPN → 清 TLB，或给 TLB 项打 **ASID**。

权限从可写改成只读、或 COW 改映射时，必须作废旧 TLB；多核还要 shootdown，否则硬件可能按旧的「可写」去写。

x86 口径：谁被换出去由 **OS 策略**决定。load/store 只有 fault/保护错才 trap，不是每次都进内核。

2026 Q30 / 2025 Q6 四句都对：

1. TLB miss 可以由硬件走页表，不必缺页
2. 普通 load/store 通常不进内核
3. 换哪一页由 OS 决定
4. 单级改多级不必然改变 TLB miss 频率

### 7.2 EAT 模板（先写清命中花多少、缺失花多少）

无缺页时：

```
Hit  = t_TLB + M                    // M = 一次内存
Miss = t_TLB + L*M + M              // L 层页表各一次，再加数据一次
EAT  = p_hit * Hit + p_miss * Miss
```

没有 TLB 时：每次都是 (L+1) 次内存。

例：80% hit，TLB 10 ns，一次内存 100 ns，两层

- Hit = 110 ns
- Miss = 10 + 2×100 + 100 = 310 ns
- EAT = 0.8×110 + 0.2×310 = 150 ns
- 若没有 TLB，每次 3 次内存 = 300 ns

**2025 Q4**（2 级页表）：

- 旧：TLB 时间≈0，hit=25% → 命中 1M，缺失 3M → EAT=2.5M
- 新：hit=50%，但 TLB 时间=M → 命中 2M，缺失 4M → EAT=3M
- 改善 = (2.5−3)/2.5 = **−20%**（变差）

**2025 Q17**：TLB=5，内存=75，找 PTE=100，无缺页

- 命中 5+75=80；缺失 5+100+75=180
- 80h + 180(1−h) ≤ 90 → **h ≥ 0.9**

**2026 Q26**：原 TLB=0.2T，新=0.1T，miss rate +10 个百分点；单级页表下 miss 多一次内存。  
EAT_old = 0.2T + mT + T  
EAT_new = 0.1T + (m+0.1)T + T  
两者相等，**EAT 不变**。

有缺页再加处理时间（题目给才加）。

---

## 8. 缺页置换（Page Replacement）

物理帧数固定，页一开始都在盘上。第一遍填满帧的每次都算缺页。画一张「帧内容 + 谁是受害者」表。

| 算法 | 换谁 | 现实性 | 注意 |
|------|------|--------|------|
| **Optimal / MIN / Belady** | 未来最久才会再用 | 需要预知未来，当缺页下界 | 现实不能用 |
| **FIFO** | 谁最早进来谁先走 | 实现简单 | 与最近用没用无关；可能有 **Belady 异常**（帧变多缺页更多） |
| **LRU** | 最久没被访问 | 比 FIFO 准，比 OPT 可实现 | 用栈/计时近似；全量 LRU 开销大 |
| **Clock / Second Chance** | 环形扫描 reference bit | 比 FIFO 好，比精确 LRU 便宜 | bit=1：清 0 给第二次机会；bit=0：换掉 |

实时+低开销场景：继续用 Clock，调 reference bit 复位频率。  
2025 Q9 = B。不要选 Optimal（没有未来知识）；不要为了少缺页去上全量 LRU。

**2025 Q21** 序列 `1 2 3 1 2 4 1 2 3`，3 帧：

- Optimal **5**，FIFO **7**，LRU **5**
- 易错：LRU 在换入 4 时换的是 3，最后 3 再缺一次，一共 **5 不是 6**

OPT/LRU 没有 Belady 异常；FIFO 可能有。

---

## 9. 内存分配器（Heap / Buddy / Slab / malloc）

### 9.1 三层不要混

| 层 | 谁用 | 干什么 |
|----|------|--------|
| **用户堆分配器** | `malloc`/`free` | 变长块，header + free list + split/coalesce |
| **Buddy（伙伴）** | 内核管物理页块 | 2^order 页，分裂/合并兄弟 |
| **Slab** | 内核小对象 | 在 buddy 给的页上切固定大小 slot |

`free` 只把块还回 **这个进程的** 分配器。进程终止 ≠ 需要 `free`；内核拆掉整个地址空间。

### 9.2 Fit 策略

| 策略 | 怎么找 | 特点 |
|------|--------|------|
| **First Fit 首次适应** | 从头找第一个够大的 | 通常最快；前面容易堆小洞 |
| **Best Fit 最佳适应** | 找最小能放下的 | 剩很多碎渣，外部碎片重，还要多搜 |
| **Worst Fit 最差适应** | 找当前最大块 | 要扫全表或按大小排序（排序本身有成本） |

Piazza Q11：Talia「谁来排序？排序有没有成本？」→ 「一开始更快」指 **First Fit**，不是 Worst Fit。2025 Q11 = E First Fit。

空闲块个数 = 连续空闲被隔成了几段，**不是** 空闲总字节。

### 9.3 malloc 到底保证什么

`malloc` 成功（非 NULL）保证的是 **虚拟地址可用**。

不保证：

1. 整块物理页已经配好（demand paging）
2. 写块内每个字节一定成功（overcommit / OOM）
3. 进程死后还占着直到 `free`（退出时内核回收全部）

2026 Q29 = D 三句全假。Talia：课上没把这三条「保证」讲死，但按原理就是 D。

一次 `malloc(1)` 可能发生的事：

- 堆里已有空间 → 纯用户态，连 `sbrk` 都没有
- 堆不够 → `sbrk`/`brk` 扩虚拟堆
- 很大一块 → `mmap`
- 物理页、置换 → 通常 **第一次写时** 才发生；题目没说内存满就不要默认走置换

2025 Q28 最稳 = D 只有 (1) sbrk（若选项语境把「第一次访问」也算进去，才可能出现缺页那条）。

### 9.4 First-Fit 例题（2026 Q9，老师后来补上调用序列，答案 A）

向 OS 一次拿 2 KiB；堆只增不减；太大就切开，低地址给用户；`free` 时和相邻空闲合并。

序列：`A=malloc(1000); B=malloc(1800); free(A); C=malloc(500); free(B);`

1. malloc(1000)：`[A1000][free1048]` 堆 2KiB，空闲 1 块
2. malloc(1800)：`[A1000][free1048][B1800][free248]` 堆 4KiB，空闲 2 块
3. free(A)：`[free2048][B1800][free248]` 堆 4KiB，空闲 2 块
4. malloc(500)：`[C500][free1548][B1800][free248]` 堆 4KiB，空闲 2 块
5. free(B)：`[C500][free3596]` 堆 **4KiB**，空闲 **1** 块

### 9.5 Buddy 例题（2026 Q28，答案 C：正好 16B 的空闲块是 0 个）

初始 256 B，请求 28, 90, 14, 6；无 header；向上取整到 2 的幂：32, 128, 16, 8。

1. 28→32：256 对半切，分走 32，剩 128, 64, 32
2. 90→128：分走 128，剩 64, 32
3. 14→16：32 切成 16+16，分走 16，剩 64, 16
4. 6→8：16 切成 8+8，分走 8，剩 **64, 8**

没有正好 16 的空闲块 → **0 个**。Buddy 内部碎片常见：要 28 却拿走 32。

---

## 10. 硬件提供的钩子（Lecture 13 总览）

| 硬件 | OS 拿它做什么 |
|------|----------------|
| CPU 特权级 + trap | 受控进入内核、系统调用、保护 |
| Timer | 抢占式调度 |
| MMU + TLB | 每进程虚拟地址空间 |
| 设备控制器 + 中断 / DMA | 文件、管道、磁盘 |
| 多核 | 真正并行（parallelism）；并发 concurrency 单核也能做 |

---

## 11. 考场速算模板与口诀

### 11.1 动手顺序（分页）

1. 写页大小 → offset 几位 → VPN 几位
2. 写 PTE 大小 → 一层几项 → 一层索引几位 → 几层
3. 把给定 VA 拆成各层索引 + offset（先写二进制再分组，不要按「看起来整齐的字节」切）
4. 问翻译：换页号、留 offset
5. 问占多少：数共享了几张表，每张整页计
6. 问最少/最多：挤在一起 vs 按扇出拆开
7. 问还能给数据用：总帧 − 所有非数据占用
8. 问时间：先判断 hit / miss / fault，再写 EAT

### 11.2 其他题型

- **调度**：先标到达；非抢占跑完再挑；抢占在到达点/时间片边界重比；平局 FCFS；TAT = finish − arrival
- **fork 树**：画出每个进程对每个 fork 的返回值；`||`/`&&` 先看左边；exec 成功 = 这棵子树后面全部砍掉
- **置换**：画帧表；OPT 看未来最远；FIFO 看进入顺序；LRU 看最近一次使用；填满前每次都缺页
- **堆**：从左到右画格子，每一步标 used/free；空闲块个数看「几段」不是总字节

### 11.3 口诀

- 声明里的 `*` 是「这是指针」；表达式里的 `*` 是「取出内容」。`&` 永远是「取地址」。
- 局部变量跟函数死；堆内存跟 `free` 死；用户只看见 VA。
- mode 是开关，code 是程序，memory 是地盘。用户不能自己拨开关，也不能踩内核地盘。
- syscall 不是 call 进内核，是 trap 进固定大门。提权靠 CPU，不靠内核先改 bit。缓冲区指针指向用户空间，内核再拷。
- fork：先克隆再看返回值。0 是孩子，非 0 是父亲。谁先跑看调度；有 pipe/wait 才有确定顺序。
- exec：换灵魂不换身份证。成功不回来；exec 自己 + 开头再 fork = 无限涨。
- pipe：空管 read 必睡；write 才能醒。
- wait：一次只收一个孩子。A 若在 wait 之后，A 不能排第一，但可以不是最后。
- printf 缓冲，write 不缓冲。
- SJF/SRTF 不公平可能饿死；FCFS/RR 最终会轮到。
- 长任务 + 热数据 = 绑核、少迁移。
- 页表项 PTE = 目录里的一行；页表页 = 目录本身占的页；数据页 = 房间。
- 多级页表开销 = 数「每一级出现了几个不同索引」。
- TLB = 最近翻译的小抄，不是数据本身。miss ≠ fault。
- First-Fit 看第一块；Buddy 必须 2 的幂。
- malloc 成功 ≠ 物理内存已到位。
- 数据页整页给数据；PTE 住在单独的页表页。

---

## 12. 错题与标准答案（已按老师澄清对齐）

### 12.1 对话里的进程题

**题 A 指针**  
`make()` 返回 `&z`，z 是局部变量。考点：`*` 与 `&`；栈生命周期；dangling pointer。正确做法：`malloc` 一块，写入后再返回堆指针，调用方 `free`。UB 发生在之后解引用，不是 return 瞬间。

**题 B syscall 两句**  
(2)(3) 都错。见第 3.4 节。改 mode bit 的是硬件。

**题 C / 2026 Q8** `fork + wait + 打印 B/C/A`

```c
if (fork()==0) { printf("B"); exit(0); }
if (fork()==0) { printf("C"); exit(0); }
wait(NULL);
printf("A");
```

进程：P0 两次 fork 都走非 0，wait 一次再打印 A；P1 只打印 B 后 exit；P2 只打印 C 后 exit。

约束：A 不可能第一个（wait 返回前至少一个孩子已经打印并 exit）。wait 只等一个，所以 A 可以夹在 B 和 C 中间。

- 可能：CAB, BCA, BAC, CBA
- 不可能：ABC, ACB（A 打头）  
答案：ABC cannot occur。

**题 D / 2026 Q10** `fork + execl("./a.out") + fork`（假设总成功）

有效逻辑 = 每次进入 `main` 都 fork，然后父子都 exec 回自己的 main。  
1 → 2 → 4 → 8 → … **无限指数增长**。  
第二句 fork 是死代码。execl 不会再开一个进程（PID 不变）。对比 `execl("/bin/ls")` 会结束。

**题 E / 2026 Q12** getpid 过滤后只打印 p  
四个进程各有一份 p。只有原进程 `getpid()==parent`，它的 p 经过两次 `++` 变成 **2**。输出确定，与调度无关。

**题 F / 2026 Q13** pipe + fork

```c
pipe(p);
if (fork()==0) { read(p[0], &x, 1); printf("B\n"); exit(0); }
printf("A\n");
write(p[1], "x", 1);
wait(NULL);
printf("C\n");
```

happens-before：父 A → 父 write → 子 read 返回 → 子 B → exit → 父 wait 返回 → 父 C。  
子进程即使先被调度，也会在空管道的 read 上睡着。只有 **ABC**。

### 12.2 2025 试卷速查

| 题 | 答案 | 备注 |
|----|------|------|
| 1 | A | pipe：trap → sys_pipe |
| 2 | B | execl 吃掉后面的 fork，剩 2 个进程 |
| 3 | B | 忙等能睡够，但占 CPU |
| 4 | E | EAT 变差 −20% |
| 5 | E | exit 后是 zombie |
| 6 | E | TLB 四句都真 |
| 7 | C | 子进程 exec 自己 |
| 8 | B | WorldHelloAgain |
| 9 | B | 调 Clock，不要 Ideal LRU / Optimal |
| 10 | B | sizeof=16 |
| 11 | E | First Fit（不是 Worst Fit） |
| 12 | D | 只打印 2 |
| 13 | B | 10 10 3 3（父 wait，顺序确定） |
| 14 | B | 页号 0xF135，选项没有 |
| 15 | D | 上下文切换 真假真 |
| 16 | D | COW：假假真 |
| 17 | C | hit ≥ 0.9 |
| 18 | D | 2^18 |
| 19 | E | 3 级 |
| 20 | D | 133 页 |
| 21 | D | OPT/FIFO/LRU = 5,7,5（LRU 不是 6） |
| 22 | 争议 | 严格不是 A；正确理由是 B 和 D |
| 23 | C | FCFS 4,10,12,13 |
| 24 | A | SJF 4,13,7,5 |
| 25 | D | SRTF 5,13,7,3 |
| 26 | D | RR3 10,13,8,9 |
| 27 | A | SRTF TAT 最短 |
| 28 | D | malloc(1) 最常只 sbrk |
| 29 | D | 11111（短路，5 个进程） |
| 30 | B | 时间片到 → 强制切换 |

### 12.3 2026 S1 Term Test 速查（老师澄清已写入）

`1 E  2 B  3 B  4 B  5 C  6 B  7 B  8 B  9 A  10 D`  
`11 B 12 B 13 B 14 E 15 C 16 D 17 D 18 C 19 E 20 A`  
`21 C 22 A 23 E 24 D 25 D 26 E 27 A 28 C 29 D 30 A`

必须记住的澄清：

- **Q5**：老师明确说 (3) 为 false，答案是 **C 不是 D**（硬件改 mode bit）。
- **Q9**：卷面漏了调用序列，老师后来补上，答案 **A**（最终 4KiB、1 个空闲块）。
- **Q23**：按 3|6|6|6|9 拆是 **6 张页表 = 3072 B = E**（不是 5 张/2560）。
- **Q24**：老师确认最大值是 39，选项 D = 14, 39。

典型错因：

| 题 | 错因 |
|----|------|
| Q1 | 以为 return 瞬间就是 UB；其实是之后解引用 |
| Q4 | 以为两个进程共享全局 g |
| Q5 | 以为内核软件改 mode bit |
| Q6 | 把 monolithic/microkernel 的性能/隔离搞反 |
| Q8 | 以为 ABC 可能（wait 在 A 前） |
| Q10 | 以为 execl 后进程数固定为 2 |
| Q13 | 忽略空管 read 阻塞 |
| Q16 | SRTF 完成时间记成另一组数据 |
| Q18 | 以为 SJF/SRTF 也能保证最终跑到 |
| Q21 | 把页表页大小写成 4KiB（本题页是 512 B） |
| Q23 | 三个地址误当成同一张 L1 |
| Q25 | sizeof 忘记最后按 max align 补齐 |
| Q28 | Buddy 以为还剩一块 16 |

### 12.4 分页易错清单

- 虚拟页和物理页 offset **相同**、数值原样拷贝
- PTE 不是 VA 的一段
- 数据页不要扣 PTE 字节
- 层数由「VPN 位数 / 一层索引位数」决定
- 不要把 VPN 当整数去除以 8 字节
- TLB miss 不是缺页
- 算进程占多少内存必须加页表页（以及题目里的内核）

---

## 13. 和作业 / 讲座的衔接

| 讲座 | 主题 |
|------|------|
| Lecture 1–2 | 三条主线、进程 |
| Lecture 7–8 | 调度 |
| Lecture 9 | 单级页表：页、帧、VPN/offset、单层浪费 |
| Lecture 10 | 多级页表 + TLB：分层、walk、EAT、ASID |
| Lecture 11 | 缺页置换 |
| Lecture 12 | 分配器与 malloc 内核实现 |
| Lecture 13 | 期中复习：翻译保持 offset；hit ≠ fault |
| Tutorial 4 | 10 数据页最少 14 / 最多 39 |

作业 A1：

- Part 1 xv6 基础：用户程序 `touch`、进程、pipe、简单 syscall
- Part 2 优先级调度器
- C 版仓库 `xv6-c-student`，构建 `make qemu`
- Rust 版 `xv6-rust-student`，构建 `cargo run --release`
- 必须全程只用一种语言
- 退出 QEMU：`Ctrl-a` 然后 `x`
- xv6 无 `ps`：C 版可用 `Ctrl-p` 打印进程
- `touch`：缺参数要报错；用 `open` 创建文件；加进 Makefile / Cargo.toml

用户态 `malloc` 最终仍可能通过 syscall 向内核要虚拟内存，缺页时再配物理页。

---

完。重做试卷时：先限时整份做完再翻本表；调度题自己画时间线；页表题先写出索引切分再填地址；堆分配从左到右画格子。Q5、Q9、Q23/Q24 以老师论坛澄清为准。
