# COMPSCI 340 A2（Rust）作业教程

这份教程记录的是：**我怎么读题、怎么在 starter 里找切入点、三题分别改哪里、以及在 QEMU 里怎么验。**  
作业虚拟机就是 QEMU 里跑的 xv6（`cargo run --release`）。FlexIT / 自己的 Ubuntu 上步骤相同。

Starter：`https://github.com/UoA-CS340-SE370-OSDev/xv6-rust-student`  
做好的代码在本仓库的 `xv6-rust-student/`。

---

## 0. 题目在问什么（先别写代码）

PDF 三题其实是同一条线：**页表 + 权限 + trap**。

| 题 | 分数 | 一句话 |
|----|------|--------|
| 1 | 15 | 走内核页表，数「有效页」，用 syscall 报出**字节数** |
| 2 | 20 | 给某一页开关写权限（`PTE_W`），非法地址要返回错误 |
| 3 | 15 | 解引用空指针时打印 `Segmentation Fault!` 并杀掉进程 |

环境注意：

- **整份作业只用 Rust**，不要混 C 版。
- **不要带 A1 的改动**。如果机器上还有上一份 xv6，删掉再重新 clone starter。
- `user/user.ld` 把程序放在地址 `0x0`。所以「空指针」并不天然无效——page 0 上往往就是 `_start`。这是 Task 3 的关键。

---

## 1. FlexIT / 本地 Ubuntu 怎么跑起来

作业 PDF 写：和 A1 同一套工具链。没有的话：

```bash
# Rust nightly + RISC-V 目标
curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh
source "$HOME/.cargo/env"
rustup toolchain install nightly
rustup target add riscv64gc-unknown-none-elf --toolchain nightly

# QEMU
sudo apt update
sudo apt install -y qemu-system-misc qemu-utils
```

拿代码（二选一）：

```bash
# 课程官方 starter（空白）
git clone https://github.com/UoA-CS340-SE370-OSDev/xv6-rust-student
cd xv6-rust-student

# 或者直接用已经做好的这份
git clone https://github.com/wumingsaberarcher/compsci340.git
cd compsci340/xv6-rust-student
```

构建、做文件系统、进 xv6 shell：

```bash
cargo build --release
qemu-img create target/fs.img 2G
./mkfs.sh
cargo run --release
```

进到 `$` 之后可以跑：

```text
used
ropagetest
nullptrtest
a2check
poweroff 0
```

`Ctrl-A` 再按 `X` 也能退出 QEMU。

---

## 2. 我怎么读 starter（先画地图）

加一个 syscall 不是只改一个文件。用户态 `ecall` 的路径是：

```text
用户程序
  -> user/src/syscall.rs   （把函数变成 ecall，syscall 号放 a7）
  -> trap.rs usertrap      （识别 EnvironmentCall）
  -> kernel/src/syscall.rs （按 a7 分发）
  -> kernel/src/sysproc.rs （真正的实现）
```

页表相关几乎都在 `kernel/src/vm.rs`：

- `PageTableEntry`：64 位 PTE。低 10 位是标志，其余是物理页号。
- `is_v` / `is_u` / `is_w` / `is_leaf`：已经写好，Task 2 只要再加 `set_w`。
- `free_walk`：递归走整棵页表树。Task 1 的 `walk_used` **照着它写**，只是计数而不是释放。
- `walk` / `walk_mut` / `walk_addr`：用虚拟地址找到那条 PTE。Task 2 用这个改权限。
- `Kvm`：内核页表。题目明确要你给它加 `pub fn get(&self) -> &PageTable`。
- `Uvm`：每个进程自己的用户页表。`mprotect` 改的是这个。
- `vmfault`：缺页 / COW。`sbrk` 是惰性分配，**先写一下**才会真正有 PTE。所以 `ropagetest` 要先 `*page = 0xFF` 再 `mprotect`。

trap 在 `kernel/src/trap.rs` 的 `usertrap()`。现在 Load/Store page fault 会先交给 `vmfault`；失败就 `killed = true`，**release 构建默认不打印**。Task 3 要补的就是「失败时说人话」。

`exec` 在 `kernel/src/exec.rs`。里面已经有栈守卫：`pagetable.clear(VA)` 清掉 `PTE_U`。Task 3 提示「去看 exec 的 page protection」，指的就是这个模式。

对照一个现成小程序最省事：`user/bin/uptime.rs`（调一个无参 syscall 再打印）。

---

## 3. 加 syscall 的固定清单（三题都用）

每加一个 syscall，这 6 处都要动。漏一处，用户态会得到 `NotImplemented` 或链接失败。

1. `kernel/src/syscall.rs` 的 `enum Syscall`：接在 `Ioctl = 23` 后面  
   `Getusedmem = 24`、`Mprotect = 25`、`Munprotect = 26`
2. 同一个文件的 `TryFrom<usize>` match
3. 同一个文件的 `syscall()` 分发
4. `kernel/src/sysproc.rs`：实现 `sys_*`
5. `user/src/syscall.rs` 的 `raw` 模块：`syscall0` / `syscall1`
6. `user/src/syscall.rs` 外面再包一层 `Result`（和 `uptime` / `sbrk` 一样）

用户 crate 通过 `kernel::abi` 共用同一个 `Syscall` 枚举，**号码必须一致**。不要在用户态自己另写一套数字。

新用户程序还要：

- 新建 `user/bin/xxx.rs`（`#![no_std]` + `#![no_main]` + `#[unsafe(no_mangle)] fn main`）
- 在 `user/Cargo.toml` 加 `[[bin]]`
- `mkfs.sh` 会把 `user/bin/*.rs` 打进 `fs.img`，所以改完程序要重新 `./mkfs.sh`

---

## 4. Task 1：`getusedmem` + `used`

### 4.1 题目要你数的是哪张表

PDF 写的是 **kernel page table**，不是当前进程的用户页表。

内核启动时 `Kvm::make()` 会 identity-map：UART、virtio、PLIC、内核代码/数据、整段物理 RAM（到 `PHYSTOP`）、trampoline、各进程内核栈。所以 `used` 打出来会是一个很大的稳定数字（我这边 release 是 **201605120 bytes**），不是「当前这个 `used` 进程占了多少」。

### 4.2 `walk_used`：抄 `free_walk` 的骨架

`free_walk` 对 512 个 PTE 循环：有效且不是叶子 → 递归进下一层。  
`walk_used` 同样走树：

- 有效 **叶子**（`PTE_R | PTE_W | PTE_X` 至少一个）：这是真正映射出去的页，`+1`
- 有效 **非叶子**：这是下一级页表页，递归进去
- 无效：跳过

最后 `sys_getusedmem` 做 `walk_used() * PGSIZE`，因为输出单位是 **bytes**。`PGSIZE` 是 4096。

`Kvm` 要加题目指定的 getter：

```rust
pub fn get(&self) -> &PageTable {
    &self.0
}
```

syscall 本体（`sysproc.rs`）：

```rust
pub fn sys_getusedmem(_args: &SyscallArgs) -> Result<usize, SysError> {
    let kvm = KVM.get().ok_or(SysError::ResourceUnavailable)?;
    Ok(kvm.get().walk_used() * PGSIZE)
}
```

评分表对得上：`used.rs` 调 `getusedmem`；`getusedmem` 调 `walk_used`；`walk_used` 走页表树；输出格式正确。

### 4.3 用户程序

`user/bin/used.rs`，格式必须是：

```text
Used memory: 201605120 bytes
```

中间一个空格，单位 `bytes`，不要自作聪明加 `KiB`。

---

## 5. Task 2：`mprotect` / `munprotect`

### 5.1 行为

- `mprotect(addr)`：把 `addr` 所在那一页的 `PTE_W` 清掉 → 只读
- `munprotect(addr)`：把 `PTE_W` 置上 → 可写
- 改完必须 `sfence.vma`，否则 TLB 还缓存旧权限，测起来会「有时灵有时不灵」

作业给的测试程序逻辑是：

1. `sbrk(100)` 拿到旧 break（exec 之后一般是页对齐的）
2. **先写一个字节**，强迫惰性分配真正建好 PTE
3. `mprotect` → 再读，应该还是 `255`
4. `munprotect` → 再写 `2`，再读应该是 `2`

如果把中间那行写打开，内核会因为写只读页而杀掉进程。作业写了：这一步不用自己再打印错误。

### 5.2 三个函数，签名按 PDF 来

`PageTableEntry`：

```rust
pub fn set_w(&mut self, flag: bool) {
    if flag { *self |= PTE_W; } else { *self &= !PTE_W; }
}
```

`PageTable`：

```rust
pub fn set_page_writeable(&mut self, va: VA, writable: bool) -> Result<(), VmError>
```

里面要检查：

1. `va` 页对齐，且 `< MAXVA`，否则 `InvalidAddress`
2. `walk_mut(va, false)` 能找到 PTE
3. PTE 有效且 `PTE_U`（用户页）。栈守卫、内核页都不能改
4. `set_w` + `sfence`

syscall（必须从进程拿**可变**页表，题目提示用 `args.proc()` 加 `unsafe`）：

```rust
let data = unsafe { args.proc().data_mut() };
data.pagetable_mut().set_page_writeable(va, writable)
```

错误码：未对齐 → `InvalidArgument`；未映射 / 非用户页 → `BadAddress`。评分要「invalid arguments return an appropriate error code」。

### 5.3 两个容易踩的坑

- **没先写就 mprotect**：`sbrk` 只增大 `proc.size`，PTE 还是无效的，`walk` 会失败。作业示例才先赋值 `0xFF`。
- **去改内核页表**：`mprotect` 改的是**当前进程用户页表**。Task 1 走 KVM，Task 2 走 UVM，别混。

---

## 6. Task 3：`Segmentation Fault!`

### 6.1 先搞清楚：Rust 版「已经有保护」是什么意思

`user/user.ld` 是 `. = 0x0`。`exec` 会把 `_start` 映射到 page 0，而且带 `PTE_R | PTE_U | PTE_X`。  
所以 debug 下 `*null` **不会缺页**，只会读到程序开头几个字节，打印一个莫名其妙的整数。

这和 Unix「地址 0 不映射，空指针 = SIGSEGV」不一样。

`exec` 里现成的保护是**栈守卫**：`clear()` 去掉 `PTE_U`。Task 3 提示去读它，是让你用同一种「改 PTE 标志」的思路，而不是去改硬件。

我的做法：

1. `exec` 加载完 ELF 之后，对 page 0 调用 `revoke_rw`：清掉 `PTE_R | PTE_W`，**留下 `PTE_X`**。  
   这样 `_start` 还能取指，但 `lw` / `sw` 地址 0 会 trap。
2. `vmfault` 里如果 `va < PGSIZE` 直接失败，避免惰性分配又把 page 0 补回来。
3. `usertrap` 里：page fault 失败且 `stval < PGSIZE`，打印 `Segmentation Fault!`，然后 `killed = true`。

`stval` 是出问题的虚拟地址；判断「是不是空指针那一页」用它，不要写死 `0` 这一个字节。

### 6.2 我实测时踩到的坑（release + LTO）

作业示例是：

```rust
let ptr: *const i32 = core::ptr::null();
unsafe { println!("{}", *ptr); }
```

`Cargo.toml` 的 release 开了 `lto = true`。LLVM 认定 `*null` 是 UB，会直接生成 **`unimp`（非法指令）**，根本不去访存。

第一次 QEMU 结果是：

```text
! unexpected interrupt scause=0x2 pid=5 sepc=0x4 stval=0x0
```

`scause = 2` 是 `IllegalInstruction`，不是 page fault。进程确实被杀了，但字不对。

所以 `usertrap` 还要匹配 `Exception::IllegalInstruction`，同样打印 `Segmentation Fault!`。  
用枚举，不要写 `if scause == 2`——评分有一项是 **Uses correct RISC-V primitives**。

本地 `scause` 模块原先没有 `InstructionPageFault`（RISC-V cause 12）。一并补上，取指落到未映射页时也能走同一套处理。

### 6.3 两条路径都要通

| 情况 | 硬件原因 | 期望 |
|------|----------|------|
| `read_volatile(0)` / debug 下的 `*null` | LoadPageFault，`stval < 4096` | `Segmentation Fault!`，exit -1 |
| release 下作业那份 `*null` | IllegalInstruction | 同上 |
| `ropagetest` 写只读页 | StorePageFault，`stval` 不是 page 0 | 杀掉即可，不必打印 |

---

## 7. 改了哪些文件（对照用）

```text
kernel/src/vm.rs         walk_used, Kvm::get, set_w, set_page_writeable,
                         revoke_rw, vmfault 拒绝 page 0
kernel/src/sysproc.rs    sys_getusedmem, sys_mprotect, sys_munprotect
kernel/src/syscall.rs    三个 syscall 号 + 分发
kernel/src/trap.rs       usertrap：空指针 / 非法指令打印 Segmentation Fault!
kernel/src/riscv.rs      InstructionPageFault
kernel/src/exec.rs       exec 后 revoke_rw(0)
user/src/syscall.rs      用户态包装
user/bin/used.rs         作业要求
user/bin/ropagetest.rs   PDF 测试程序
user/bin/nullptrtest.rs  PDF 测试程序
user/bin/a2check.rs      额外：非法参数、写保护页、volatile 空指针
user/Cargo.toml          注册上面几个 bin
create-submission.sh     打 CS340_A2_RUST_<UPI>.zip
```

---

## 8. 在 QEMU 里怎么验（我这边的结果）

```text
$ used
Used memory: 201605120 bytes

$ ropagetest
255
2

$ nullptrtest
Segmentation Fault!

$ echo still-alive
still-alive

$ a2check
unaligned: err
unmapped: err
mprotect-write: status -1
Segmentation Fault!
null-load: status -1
```

`nullptrtest` 之后 shell 还能跑，说明只杀了出错的进程，内核没崩。

回归：`./test.sh`（fs / pipe / proc / fd / sbrk / cow）应全部 `ok`。  
`sbrk` 和 `cow` 特别值得跑，因为我们动了 `vmfault` 和 PTE 写权限。

数字 `201605120` 取决于 `PHYSTOP`（这份 starter 是 `KERNBASE + 128MiB`）加上 PLIC 等设备映射，换配置可能会变。评分看的是格式和「走了页表」，不是这个常数。

---

## 9. 打包提交

```bash
cd xv6-rust-student
sh create-submission.sh          # 默认 tli637
# 或
sh create-submission.sh 你的upi
```

得到 `CS340_A2_RUST_<UPI>.zip`，里面是 `kernel/` 和 `user/`，上传 Canvas。  
不要把 `target/` 打进去。

---

## 10. 做的时候可以对照的评分表

**Task 1**

- [x] `used.rs` 调用 `getusedmem`
- [x] syscall 调用 `walk_used`
- [x] `walk_used` 递归走页表树
- [x] 输出 `Used memory: … bytes`

**Task 2**

- [x] 两个 syscall，签名在 `sysproc.rs`
- [x] `set_page_writeable` 在 `vm.rs`
- [x] 非法参数返回错误码
- [x] `ropagetest` 打印 `255` / `2`

**Task 3**

- [x] 扩展的是 `usertrap`（以及 `exec` 的页权限）
- [x] 用 `scause::Exception::…`，不写魔数
- [x] 杀掉解引用空指针的进程
- [x] 输出恰好是 `Segmentation Fault!`

---

## 11. 如果 FlexIT 上复现失败

| 现象 | 多半原因 |
|------|----------|
| `used: exec failed` / 没有这个命令 | 忘了改 `Cargo.toml` 或没重新 `./mkfs.sh` |
| `function not implemented` | `Syscall` 枚举 / `TryFrom` / 分发漏了一处 |
| `ropagetest` 一 mprotect 就死 | 没先写那一页，PTE 还不存在 |
| `ropagetest` mprotect 之后还能写 | 忘了 `sfence.vma` |
| `nullptrtest` 打出一个整数 | page 0 仍可读；检查 `exec` 里的 `revoke_rw` |
| `nullptrtest` 打 `unexpected interrupt scause=0x2` | 只处理了 page fault，没处理 release 的 `IllegalInstruction` |
| `./test.sh` 的 cow / sbrk 挂了 | `vmfault` 把不该拒绝的地址拒了；`va < PGSIZE` 只要拦 page 0 |

作业是 Lane 2，可以用 AI，但交上去的 zip 必须是你自己能讲清楚的那份。对照第 10 节，每一项都能指到具体函数即可。
